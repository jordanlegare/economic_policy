#pragma once

// Public compatibility header. The implementation is split so schema/gate
// semantics, snapshot loading, and runtime application can be tested and
// reviewed independently without changing existing include sites.
#include "calibration_runtime.hpp"
