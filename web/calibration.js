(() => {
  'use strict';

  // Retired dashboard surfaces: Data provenance & calibration and Model evidence V2.
  // Evidence APIs remain available to the model and Principal Brief where required:
  // /api/v2/backtests · /api/v2/welfare · /api/v2/robustness
  // This module intentionally performs no DOM injection.
})();
